UPDATE rota
SET
    senha_acesso = 'poke-ditto-123',
    ultimaalteracao = current_timestamp
WHERE metodo = 1
  AND caminho = 'examples/pokemon/ditto'
  AND COALESCE(senha_acesso, '') <> 'poke-ditto-123';

UPDATE rota
SET
    senha_acesso = 'agify-cache-123',
    ultimaalteracao = current_timestamp
WHERE metodo = 1
  AND caminho = 'examples/agify/maria/cache'
  AND COALESCE(senha_acesso, '') <> 'agify-cache-123';
